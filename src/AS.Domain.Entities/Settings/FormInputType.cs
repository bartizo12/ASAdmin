namespace AS.Domain.Entities
{
    /// <summary>
    /// Input types of the settings fields"
    /// </summary>
    public enum FormInputType
    {
        Text, //Default
        Email,
        Url,
        MultiLine,
        Checkbox,
        Password,
        DigitOnly,
        Html
    }
}